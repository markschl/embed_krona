---
output:
  pdf_document: default
  word_document: default
  html_document: default
---

```{r setup}
library(phyloseq)
data(GlobalPatterns)

source('embed_krona.R')
```


```{r fig.cap='Overview of the GlobalPatterns dataset'}
# if an up to date Krona is already installed and ktImportText is available in path:
#plot_krona(GlobalPatterns)

# otherwise, manually install KronaTools in the embed_krona directory and supply its path
plot_krona(GlobalPatterns, install_dir='.')

# Note: the size of snapshots is best set using the snapshot_dim argument, but it
# is also possible to set the chunk option out.width='100%' for similar behavior
# across file formats.
```

